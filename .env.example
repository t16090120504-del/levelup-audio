VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
CREEM_API_KEY=creem_test_4nPZ2hNUXjl73WgrHy1wY1

# --- Server-side only (Netlify Functions / Cloudflare Worker) ---
# Do NOT prefix these with VITE_ — they must never be exposed to the browser.
# Set them in: Netlify (Site settings > Environment variables) and/or
# Cloudflare (Workers > Settings > Variables).

# Webhook signing secret from the Creem dashboard (Developers > Webhook).
CREEM_WEBHOOK_SECRET=your-creem-webhook-secret

# Supabase service role key (bypasses RLS — server-side use only).
SUPABASE_URL=https://ihfaoksiurmucryfzfvd.supabase.co
SUPABASE_SERVICE_KEY=sb_secret_HR5YZxJ2j0PmWExOQySlTA_hBc_B1Hy
